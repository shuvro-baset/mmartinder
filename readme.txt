1. At first please create an environment by following command
------ "python -m venv (env_name)"
2. put the project folder in the environment directory.
3. "pip install -r requirements.txt" run this command on your terminal
4. You can run every file.